create table attendance
    -> (
    -> attend_id int PRIMARY KEY auto_increment,
    -> attend_date DATE,
    -> present boolean,
    -> emp_id int not null,
    -> FOREIGN KEY(emp_id)
    -> REFERENCES empDetails(emp_id)
    -> ON UPDATE CASCADE
    -> ON DELETE RESTRICT
    -> );
